@model IEnumerable<TCC.Models.Compra>

@{
    ViewBag.Title = "List";
}

<h2>List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NumCompra)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CodigoBarras)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NomeForn)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.QtdEntraIngre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DataHoraChegada)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumCompra)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CodigoBarras)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NomeForn)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QtdEntraIngre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataHoraChegada)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { NumCompra = item.NumCompra }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }
    @Html.Raw(TempData["msg"])
</table>
